## Process the map

Can now store the map using map width.
will use the matrix to find the character looping through the map matrix.

allocate the map matrix
fill it

check for valid char
	handle all errors
save player char and position
